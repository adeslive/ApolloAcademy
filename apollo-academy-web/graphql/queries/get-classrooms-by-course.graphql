query getClassroomsByCourse($id: ID!) {
  classroomsByCourse(id: $id) {
    id
    description
    capacity
    link
    
    course {
      ...RegularCourse
    }
    
    teacher{
      id
      user {
        ...RegularUser
      }
    }
  }
}